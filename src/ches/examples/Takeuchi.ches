class examples.Takeuchi


void @entry()
    tarai(0, 1, 2)
end

int tarai(int x, int y, int z)
    if x <= y
        ret y
    else
        ret .tarai(tarai(x - 1, y, z), tarai(y - 1, z, x), tarai(z - 1, x, y))
    end
end
