; マジックナンバ
GANTCCBC

; コードネーム
FANTASIA

; タイムスタンプ
606801F2

; 関数 ID データ
<entry_func_id> @entry(str[] args)

; グローバル変数 ID データ
<str_hello_id> str "Hello!"

; @entry(str[] args)
<entry_func_id>:

; args の要素数を比較

    spush_arg   0
    spush_mem   len

    spush_i32   2

    cmp_i32

    goto_if     ?

; println(str) 呼び出し

    spush_ref   <str_hello_id>

    ; args[2] を取得

    spush_arg   0
    spush_arr   2

    

    spush_uuid  <println_func_id>
    call
